{
    "builds": [{
        "src": "base/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" ,"runtime":"Python 3.10"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "base/wsgi.py"
        }
    ]
}