{
    "builds": [{
        "src": "base/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" ,"runtime":"Python 3.10"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "base/wsgi.py"
        }
    ]
}